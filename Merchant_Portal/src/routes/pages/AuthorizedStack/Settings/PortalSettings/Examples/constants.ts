import { COMPONENTS_NAMES } from 'theme/constants';

export const previewComponents = [
  COMPONENTS_NAMES.BUTTON,
  COMPONENTS_NAMES.TYPOGRAPHY,
  COMPONENTS_NAMES.PREVIEW_APP,
  COMPONENTS_NAMES.INPUT,
  COMPONENTS_NAMES.CHECKBOX,
  COMPONENTS_NAMES.AUTOCOMPLETE_SELECT,
  COMPONENTS_NAMES.TAG,
];

export const settingsPreviewTheme = {
  examples: {
    container: {
      bg: 'palette.surface.2',
      borderRadius: '12px',
    },
    title: 'palette.primary.99',
  },
};
